# ─── Production split ───
# Site (GitHub Pages): www.getldr.ca — Vite build with VITE_API_URL + VITE_* secrets
# API (Render): https://lazarus-4uxi.onrender.com — npm start, health /api/health
#
# Render Dashboard env (no trailing slashes):
#   GEMINI_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, PURGE_CRON_SECRET, DATA_RETENTION_DAYS
#   FRONTEND_ORIGIN=https://www.getldr.ca,https://getldr.ca,http://localhost:5173
#   PUBLIC_API_URL=https://lazarus-4uxi.onrender.com
#   LAZARUS_API_KEY=<same random string as GitHub secret VITE_LAZARUS_API_KEY>
# Leave VITE_API_URL unset on Render (optional fallback UI uses same-origin /api).
# Local Vite proxies /api to :3001. GitHub Pages sets VITE_API_URL at build time.
#
# GitHub Actions: VITE_API_URL, VITE_LAZARUS_API_KEY, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
# Purge cron: LAZARUS_API_URL=https://lazarus-4uxi.onrender.com (same PURGE_CRON_SECRET)

# ─── Local full stack (API + UI on this machine) ───
# Run: npm run dev
# GEMINI_API_KEY must be a valid AI Studio key (starts with AIzaSy)

GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MODEL_ENTRY=gemini-2.5-pro
# GEMINI_MODEL_TEAM=gemini-3.1-pro-preview

ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here
# ASSEMBLYAI_SPEAKERS=2

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Guest freemium — client locks at 5; server soft daily cap (default 10/day)
# Only joshua.bennett003@gmail.com (founder) / FOUNDER_EMAILS+OPS_EMAILS skip the cap.
# GUEST_ANALYSIS_DAILY_LIMIT=10
# Allow X-Lazarus-Demo-Bypass in production (local/non-prod already allows it)
# GUEST_USAGE_DEMO_BYPASS=true
# Client-only unlock for shared demo machines (or open app with ?demo=1)
# VITE_GUEST_USAGE_BYPASS=true
# Optional extra founder emails (comma-separated) for client-side unlimited:
# VITE_FOUNDER_EMAILS=joshua.bennett003@gmail.com

# Data retention (days) — transcript_text nulled after this window; analysis_json kept
# DATA_RETENTION_DAYS=30
# PURGE_CRON_SECRET=random-secret-for-scheduled-purge-cron

# Production API auth — use the same value for both (generate a long random string)
# LAZARUS_API_KEY=your_production_api_key_here
# VITE_LAZARUS_API_KEY=your_production_api_key_here

PORT=3001

# FRONTEND_ORIGIN=https://www.getldr.ca,https://getldr.ca,http://localhost:5173

# Windows TLS (if npm install or Gemini fetch fails):
# powershell -File scripts/export-windows-cas.ps1

# ─── Zoom RTMS (live meeting transcripts) ───
# Create a Zoom General App with Realtime Media Streams at https://marketplace.zoom.us/
# ZOOM_CLIENT_ID=your_zoom_client_id
# ZOOM_CLIENT_SECRET=your_zoom_client_secret
# ZOOM_WEBHOOK_SECRET_TOKEN=from_zoom_webhook_settings
# ZOOM_REDIRECT_URI=https://lazarus-4uxi.onrender.com/api/integrations/zoom/callback
# PUBLIC_API_URL=https://lazarus-4uxi.onrender.com
# Webhook URL: https://lazarus-4uxi.onrender.com/api/webhooks/zoom
# Subscribe to: meeting.rtms_started, meeting.rtms_stopped
# Also set ZM_RTMS_CLIENT / ZM_RTMS_SECRET (same as above) on Render for @zoom/rtms SDK

# ─── Google Meet / Workspace / Gmail ───
# docs/google-meet-setup.md
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Local demo (must also be listed under Authorized redirect URIs):
# GOOGLE_REDIRECT_URI=http://localhost:3001/api/integrations/google/callback
# Production:
# GOOGLE_REDIRECT_URI=https://lazarus-4uxi.onrender.com/api/integrations/google/callback
#
# Enable Gmail API + add scope gmail.readonly on the OAuth consent screen.
# Existing connected users must reconnect once after enabling mail scopes.
# Add BOTH localhost and production redirect URIs on the Google OAuth client.

# ─── Microsoft Teams / Outlook (Entra ID / Graph) ───
# docs/teams-setup.md
# Add Microsoft Graph Mail.Read delegated permission; reconnect after changing scopes.
# TEAMS_CLIENT_ID=
# TEAMS_CLIENT_SECRET=
# TEAMS_TENANT_ID=common
# TEAMS_REDIRECT_URI=https://lazarus-4uxi.onrender.com/api/integrations/teams/callback

# ─── HubSpot (OAuth — deal search, note import, Push to HubSpot) ───
# Setup guide: docs/hubspot-setup.md
# Create a HubSpot public OAuth app with these scopes:
#   oauth, crm.objects.deals.read, crm.objects.deals.write, crm.objects.notes.write
# (crm.objects.notes.read is rejected on developer platform 2026.03 — deal notes use deals.read)
# Redirect URL must match HUBSPOT_REDIRECT_URI exactly (add BOTH local + Render in the app).
#   http://localhost:3001/api/integrations/hubspot/callback
#   https://lazarus-4uxi.onrender.com/api/integrations/hubspot/callback
# Tokens stored in .data/hubspot-tokens.json (single-process; same pattern as Google/Teams).
# Apply locally: npm run hubspot:env -- --id <id> --secret <secret>
# Live smoke (API running + connected): npm run test:hubspot:live
# Optional webhook ingest: HUBSPOT_WEBHOOK_SECRET for POST /api/webhooks/hubspot
# HUBSPOT_CLIENT_ID=
# HUBSPOT_CLIENT_SECRET=
# HUBSPOT_REDIRECT_URI=https://lazarus-4uxi.onrender.com/api/integrations/hubspot/callback
# HUBSPOT_WEBHOOK_SECRET=
# OAUTH_STATE_SECRET=random-secret-shared-across-oauth-providers

# ─── Salesforce (OAuth — opportunity search, import, Push) ───
# Connected App with api + refresh_token scopes.
# SALESFORCE_CLIENT_ID=
# SALESFORCE_CLIENT_SECRET=
# SALESFORCE_REDIRECT_URI=https://lazarus-4uxi.onrender.com/api/integrations/salesforce/callback
# SALESFORCE_LOGIN_URL=https://login.salesforce.com
# SALESFORCE_WEBHOOK_SECRET=

# ─── WhiteWhale (paused — do not enable until Lazarus has a paid license) ───
# Integration code is disabled in server/integrations/whitewhale/config.ts.
# WHITE_WHALE_API_KEY=
# WHITE_WHALE_USER_EMAIL=
# WHITE_WHALE_BASE_URL=

# ─── Supabase Auth (login UI — email magic link + Google + CRM bridge) ───
# Full guide: docs/auth-setup.md
# Mirror URL/anon key for Vite (required for login gate — restart npm run dev after set):
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_key_here
# Enable Email + Google in Supabase Auth dashboard (or: npm run auth:configure with SUPABASE_ACCESS_TOKEN).
# Google Cloud must allow redirect: https://YOUR_REF.supabase.co/auth/v1/callback
# CRM “Sign in with HubSpot/Salesforce” uses SUPABASE_SERVICE_ROLE_KEY to mint sessions.
# Optional Management API token for scripts/configure-supabase-auth.mjs:
# SUPABASE_ACCESS_TOKEN=sbp_...

# ─── Ops Command Center (founder / hire) ───
# docs/auth-setup.md — grant role: npm run auth:set-founder -- you@company.com
# After login, FOUNDER_EMAILS / OPS_EMAILS (or app_metadata.role) unlock Ops HQ
# and show the "Under the hood" button in the product header.
# FOUNDER_EMAILS=joshua.bennett003@gmail.com
# OPS_EMAILS=
# FOUNDER_ALERT_EMAILS=joshua.bennett003@gmail.com
# RESEND_API_KEY=re_...
# FOUNDER_ALERT_FROM=Lazarus Ops <onboarding@resend.dev>
# Landing contact form (same Resend key). Defaults: joshua@ / sales@ / support@getldr.ca
# CONTACT_FROM=Lazarus Deal Recovery <support@getldr.ca>
# CONTACT_EMAIL_FEEDBACK=joshua@getldr.ca
# CONTACT_EMAIL_SALES=sales@getldr.ca
# CONTACT_EMAIL_SUPPORT=support@getldr.ca
# CONTACT_EMAIL_TECHNICAL=support@getldr.ca
# FOUNDER_ALERT_TZ=America/New_York
# FOUNDER_ALERT_HOURS=8,13,20
# Optional separate cron secret (defaults to PURGE_CRON_SECRET):
# FOUNDER_ALERT_CRON_SECRET=

# ─── Stripe billing (paywall after 5 free analyses) ───
# Setup: docs/billing-setup.md
# Checkout is Stripe Payment Links on the public site (src/lib/site.ts). Customer Portal still uses the API.
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...  # optional (hosted Checkout does not need it)
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_PPU=price_...          # $10 one-time
# STRIPE_PRICE_ENTRY=price_...        # $99/month · 20 analyses
# STRIPE_PRICE_TEAM=price_...         # $499/month · unlimited
# Webhook URL (Render): https://<host>/api/billing/webhook
# Events: checkout.session.completed, customer.subscription.*, invoice.paid