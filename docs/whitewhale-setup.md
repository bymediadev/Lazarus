# WhiteWhale — paused

Lazarus is **not licensed** to use WhiteWhale. Do not set `WHITE_WHALE_*` env vars and do not call their API.

The integration code still exists under `server/integrations/whitewhale/` but is disabled in `config.ts` (`WHITE_WHALE_LICENSED = false`). Turn that back on only after a paid license is in place, then restore customer copy, legal sub-processors, and Team-plan packaging.
